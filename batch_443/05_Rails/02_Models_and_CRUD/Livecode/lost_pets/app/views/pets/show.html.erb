<h1><%= @pet.name %> (<%= @pet.species %>) </h1>
<p>Found <%= @pet.since_found %> at <%= @pet.address %></p>


<%= link_to 'Edit', edit_pet_path(@pet) %>
<%= link_to 'Delete', pet_path(@pet), method: 'delete', data: { confirm: 'Are you sure to delete?' } %>